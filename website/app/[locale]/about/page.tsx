import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { AboutPage } from "../../../components/editorial-pages"; import { isLocale } from "../../../lib/i18n"; import { pageMetadata } from "../../../lib/seo";
type Props={params:Promise<{locale:string}>}; export function generateStaticParams(){return [{locale:"ro"},{locale:"de"}]};
export async function generateMetadata({params}:Props):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale)||locale==="en")return{};return locale==="ro"?pageMetadata({locale,path:"/about",title:"Despre noi",description:"Principiile de produs, inginerie și cercetare din spatele 360Configurator."}):pageMetadata({locale,path:"/about",title:"Über uns",description:"Produktdenken, technische Prinzipien und Forschung hinter 360Configurator."})}
export default async function Page({params}:Props){const{locale}=await params;if(!isLocale(locale)||locale==="en")notFound();return <AboutPage locale={locale}/>}
