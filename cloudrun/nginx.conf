events {}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;

    server {
        listen 8080;
        server_name _;

        root /usr/share/nginx/html;
        index index.html;

        # Solar configurator -> Overpass proxy.
        # The browser calls these same-origin endpoints, so Overpass CORS does not
        # affect the configurator. Two endpoints are kept so the frontend can retry
        # the secondary public Overpass instance if the primary one fails.
        location = /api/solar/overpass-primary {
            proxy_pass https://overpass-api.de/api/interpreter;
            proxy_http_version 1.1;
            proxy_ssl_server_name on;
            proxy_ssl_name overpass-api.de;
            proxy_set_header Host overpass-api.de;
            proxy_set_header Connection "";
            proxy_set_header User-Agent "360Configurator/1.0 (+https://www.360configurator.com)";
            proxy_set_header Accept "application/json";
            proxy_set_header Content-Type "application/x-www-form-urlencoded";
            proxy_connect_timeout 10s;
            proxy_send_timeout 15s;
            proxy_read_timeout 30s;
            proxy_buffering off;
        }

        location = /api/solar/overpass-secondary {
            proxy_pass https://overpass.kumi.systems/api/interpreter;
            proxy_http_version 1.1;
            proxy_ssl_server_name on;
            proxy_ssl_name overpass.kumi.systems;
            proxy_set_header Host overpass.kumi.systems;
            proxy_set_header Connection "";
            proxy_set_header User-Agent "360Configurator/1.0 (+https://www.360configurator.com)";
            proxy_set_header Accept "application/json";
            proxy_set_header Content-Type "application/x-www-form-urlencoded";
            proxy_connect_timeout 10s;
            proxy_send_timeout 15s;
            proxy_read_timeout 30s;
            proxy_buffering off;
        }

        # Window configurator
        location = /window-configurator {
            return 301 /window-configurator/;
        }

        location /window-configurator/ {
            try_files $uri $uri/ /window-configurator/index.html;
        }

        # Pergola configurator
        location = /pergola-configurator {
            return 301 /pergola-configurator/;
        }

        location /pergola-configurator/ {
            try_files $uri $uri/ /pergola-configurator/index.html;
        }

        # Roof configurator
        location = /roof-configurator {
            return 301 /roof-configurator/;
        }

        location /roof-configurator/ {
            try_files $uri $uri/ /roof-configurator/index.html;
        }

        # Solar configurator
        location = /solar-configurator {
            return 301 /solar-configurator/;
        }

        location /solar-configurator/ {
            try_files $uri $uri/ /solar-configurator/index.html;
        }

        # Hall configurator
        location = /hall-configurator {
            return 301 /hall-configurator/;
        }

        location /hall-configurator/ {
            try_files $uri $uri/ /hall-configurator/index.html;
        }

        # Shared UI used by standalone configurators
        location /shared-ui/ {
            try_files $uri =404;
        }

        # Main website
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
